# Project Memory

- Before coding, read the relevant files in `docs/conventions/`.
- When continuing prior work, read the latest entries in `docs/dev-journal.md` and any matching `docs/tasks/` note.
- After a non-trivial change, update reusable conventions and prepend a concise journal entry.
